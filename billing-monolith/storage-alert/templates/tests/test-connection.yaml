apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "storage-alert.fullname" . }}-test-connection"
  labels:
    {{- include "storage-alert.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "storage-alert.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
