apiVersion: v1
kind: Service
metadata:
  name: {{ include "queue-service-consumer.fullname" . }}
  labels:
    {{- include "queue-service-consumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "queue-service-consumer.selectorLabels" . | nindent 4 }}
